export default function Loading() {
  return (
    <div className="fixed inset-0 z-[9999] flex items-center justify-center bg-coral">
      {/* IntroSplash handles branded intro motion on top. Loading shell ensures 0ms white flash during server computation */}
    </div>
  );
}
