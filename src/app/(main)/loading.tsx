export default function MainLoading() {
  return (
    <div className="fixed inset-0 z-[9999] flex items-center justify-center bg-coral">
      {/* Ensures zero white flash when navigating to /home */}
    </div>
  );
}
